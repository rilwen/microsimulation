Import('env')
Import('core')
Import('testing')
Import('gtest')
Import('GTEST_LIBS')
Import('OTHER_LIBS')
penv = env.Clone()
sources = Glob('*.cpp')
penv.Append(LIBS= [OTHER_LIBS] + GTEST_LIBS)
tests = penv.Program('tests', sources + core + testing + gtest)
Return('tests')
